# 安全护栏 · Guardwall 技能说明

本技能随 dsh-guardwall 插件加载。它的职责是让 Agent 在 dsh 中具备
「安全边界意识」，配合护栏完成安全操作，而不是绕过护栏。

## 被拦截时怎么办

当工具调用返回 GuardwallBlock 错误（`blocked by dsh-guardwall rule SEC-xxx`）：

1. **先停止**，不要重试、不要换参数绕开。
2. 向用户说明拦截原因（规则 ID + 摘要）：
   「安全护栏拦截了读取 SSH 私钥的请求（SEC-002，风险 9/10）。」
3. 如果这是用户明确授权的合法操作：
   - `guard_whitelist` 默认由管理员关闭；不能要求 Agent 自行开启
   - 管理员已显式开启时，请用户确认后只放行精确工具名
     （rule=SEC-002, tool=具体工具名，minutes=1–60 分钟）
   - 放行后正常执行，并在结果里提醒用户「已临时放行，注意日志留痕」
4. 如果用户没有明确授权：**不要继续**，换安全做法
   （如读 .env 前先问用户、不把密钥写进命令参数、用环境变量引用）。

## 输出泄露提示

当 `tools/result` 审计命中 OUT 规则（如密钥/内网 IP/数据库串），
会记入审计日志并告警。Agent 在生成回复时应：
- 对密钥类内容打码（只保留前后 4 字符）
- 内网 IP 如非必要不展示
- 提醒用户「该结果含敏感信息，已记录审计」

## 用户问护栏状态时

调用 `guard_status` 返回：今日拦截/告警/记录统计、最近事件、
审计链完整性（ok:true 表示无篡改）、白名单、策略阈值。

## 边界

- 护栏是**协作方**不是敌人：命中告警时要解释清楚，不要假装没发生。
- 用户明确要求放行的合法操作，放行后照常执行，但要留痕。
- 本技能不阻止用户主动查看 `guard_status` 或修改策略配置。

## 热加载规则（v0.4）

用户说"加一条规则""改拦截阈值"时：
- 自定义规则写入 `~/.dsh/cache/dsh-guardwall/rules.d/*.json`（保存即生效）
- 阈值写入同目录 `config.json`（`{"blockThreshold":6,"warnThreshold":3}`）
- 用 `guard_reload` 强制刷新，用 `guard_rules` 确认新规则已在生效列表里
